# TODO

## 界面

- 标题：CMSIS-DAP Flash Tool
- 从上到下由两个区域构成。
- 界面整洁简单，偏工业风，没有额外装饰。

### 上方区域：设备管理

- 一个滑动列表列出已配对设备，显示设备名、VID和PID，是否支持JTAG和SWD，可以选中。
- 列表下方有四个按钮：配对、打开、关闭，按钮之间根据功能有互锁关系。
- 再下面一行字显示是否打开完成，最左侧还有一个红/绿色的圆点提示状态。

### 下方区域：FLASH操作

- 一个下拉列表，时钟速度选择列表，100、500KHz，1、2、5、10、20MHz可选，默认是10MHz。
- 下面是一个下拉列表，选择FLASH型号，型号列表预先存在代码里，最好是在一个单独的文件里。
  - 一个FLASH型号存储的信息有：厂商、型号、大小等。。。后期还要添加。
- 上述两个列表的右边有一个自动匹配的按钮，点击后会以低速自动读取FLASH的JEDEC id，在列表中匹配型号，还会调整速度。
- 再下面是一个大的数据预览区域，以16进制展示数据缓冲区内的数据，有滚动条可以浏览。
  - 每行左侧显示地址，低4位固定是0。上方显示地址的低4位，0-F，显示效果类似表格。
- 数据预览区域右侧从上到下是几个按钮：打开、保存、读取、擦除、查空、写入、校验。
- 再下方是一个类似命令行的区域，显示设备接入、操作结果等日志。

## FLASH操作

### JTAG与SPI

- TCK=>SCK, TMS=>CS, TDO=>MISO, TDI=>MOSI
- JTAG_seq命令代替SPI读写。
  - 可能需要字节序(LSB/MSB)
  - 简单配置使用单个指令单包的形式。
  - 数据读写根据CMSIS-DAP设备的包长，自动分包。
